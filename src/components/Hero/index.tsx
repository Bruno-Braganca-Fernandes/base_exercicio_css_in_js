import { HeroSection, HeroContent, HeroTitle } from './style'

const Hero = () => (
  <HeroSection>
    <HeroContent className="container">
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </HeroContent>
  </HeroSection>
)

export default Hero
